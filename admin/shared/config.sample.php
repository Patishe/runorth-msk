<?php
/**
 * Конфигурация админки «Готовые объекты».
 *
 * НА СЕРВЕРЕ скопируйте этот файл в config.php и поменяйте пароль:
 *   1) Сгенерируйте хэш своего пароля, выполнив на хостинге (или локально):
 *        php -r 'echo password_hash("ВАШ_ПАРОЛЬ", PASSWORD_DEFAULT), "\n";'
 *   2) Вставьте полученную строку в ADMIN_PASSWORD_HASH ниже.
 *
 * config.php НЕ попадает в git и НЕ выгружается на FTP — он живёт только на сервере.
 */

return [
    // Логин в админку
    'username' => 'admin',
    // Хэш пароля (bcrypt). Здесь — заглушка, обязательно замените на сервере.
    'password_hash' => 'СГЕНЕРИРУЙТЕ_ХЭШ_СВОЕГО_ПАРОЛЯ',
    // Соль для cookie-сессии (любая длинная случайная строка)
    'session_secret' => 'change-this-to-a-random-string',
];
